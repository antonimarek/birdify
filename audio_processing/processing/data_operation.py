from pydub import AudioSegment
import librosa
import sox

# input column with paths, in return columns with all added info (True -> return particular, False -> don't)

# sample_rate = sox.file_info.sample_rate('path/to/file.mp3')
